function findSpringForDuration(
  duration: number,
  dampingRatio: number
): SpringConfig {
  // try every spring of a certain stiffness -> ratio and search for spring based on duration
}

findSpringForDuration({
  duration: 2,
  dampingRatio: 0.8
});
