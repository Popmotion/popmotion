// import { PopmotionPoserConfig } from '../types';
// import { PoseMap } from '../../../pose-core/src/types';
// import { HotSubscription } from 'popmotion/reactions/types';

// export default ({ draggable, poses }: PopmotionPoserConfig) => {
//   let newPoses: PoseMap<HotSubscription> = {...poses};

//   if (draggable) {

//   }

//   Object.keys(newPoses).forEach(key => {
//     const pose = poses[key];
//     poses[key] = pose.transition ? pose
//   });

//   return newPoses;
// };
