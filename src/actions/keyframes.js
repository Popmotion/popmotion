// import chain from './chain';
// import colorTween from './color-tween';
// import tween from './tween';
// import { color } from '../value-types';

// export default (positions, onUpdate) => {
//   const isColor = color.test(positions[0]);
//   const action = isColor ? colorTween : tween;
//   const sequence = chain(

//   );

//   return sequence;

//   return chain(
//     positions.map(() => {

//     }),

//   );
// };


// keyframes([0, 200, 300], )
