"use strict";

var defaults = {
        color: {
            min: 0,
            max: 255,
            round: true
        },
        opacity: {
            min: 0,
            max: 1
        }
    };
    
defaults.Alpha = defaults.opacity;

module.exports = defaults;