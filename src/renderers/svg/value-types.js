import { alpha, color, complex, scale } from '../../inc/value-types';

export default {
  fill: color,
  stroke: color,
  scale: scale,
  scaleX: scale,
  scaleY: scale,
  d: complex,
  points: complex,
  opacity: alpha,
  fillOpacity: alpha,
  strokeOpacity: alpha
};